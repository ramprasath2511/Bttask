
import 'package:bttask/Pages/home_screen.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:bttask/main.dart';

void main() {
  testWidgets("BT Football", (WidgetTester tester) async {
    //find all widgets needed
    // final listview = find.byKey(const ValueKey("Season"));
    //
    // //execute the actual test
    // await tester.pumpWidget(const MaterialApp(home: MatchListScreen()));
    //
    // await tester.pump(const Duration(seconds: 20));
    // //check outputs
    // expect(find.text("Season"), findsOneWidget);
  });
}
